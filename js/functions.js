function greet(name) {
	console.log(`Good Morning, ${name}`);
}

greet('Sourabh');

function addNumbers(num1, num2) {
	console.log(`${num1} + ${num2} = ${num1 + num2}`);
}

addNumbers(2, 10);